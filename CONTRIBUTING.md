# Contributing

1. Fork the repository and create a focused branch.
2. Install development dependencies with `python -m pip install -e ".[dev]"`.
3. Run `ruff check .` and `pytest`.
4. Do not commit cookies, tokens, downloaded audio, or private research outputs.
5. Open a pull request describing the behavior change and validation performed.
